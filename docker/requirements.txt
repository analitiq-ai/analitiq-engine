# Analitiq Stream - Data Streaming Framework Requirements
# Generated from pyproject.toml for Docker deployment

# Core async and concurrency
aiofiles>=23.0.0

# Database connectors
asyncpg>=0.28.0
aiomysql>=0.2.0
# PyMySQL 1.2 changed Connection.ping()'s default from reconnect=True to
# reconnect=False. SQLAlchemy 2.x's pool_pre_ping path uses that default
# to decide whether to send `False` to ping(), but the aiomysql adapter
# (AsyncAdapt_aiomysql_connection.ping) still requires the positional
# `reconnect` argument — so do_ping() calls ping() with no args and
# crashes with TypeError. Pin to <1.2 until aiomysql ships a fix.
PyMySQL>=1.0,<1.2

# API connectors
aiohttp>=3.8.0
aiohttp-retry>=2.8.3

# Data processing and validation
pydantic>=2.0.0
jsonschema>=4.0.0

# Retry and fault tolerance
backoff>=2.0.0

# Logging and monitoring
structlog>=23.0.0
prometheus-client>=0.17.0

# Configuration and utilities
python-dotenv>=1.0.0
python-dateutil>=2.9.0
click>=8.0.0
pyyaml>=6.0.0

# Fast JSON serialization (used by models)
orjson>=3.9.0

# gRPC for destination streaming
grpcio>=1.60.0
grpcio-tools>=1.60.0
protobuf>=4.25.0

# Serialization formats for gRPC payloads
msgpack>=1.0.0

# SQLAlchemy for database abstraction
sqlalchemy[asyncio]>=2.0.0

# Optional dependencies for specific use cases
# Install with: pip install "analitiq-stream[kafka]" etc.

# For Apache Kafka integration
# kafka-python>=2.0.0
# aiokafka>=0.8.0

# For cloud storage connectors  
# boto3>=1.26.0
# azure-storage-blob>=12.0.0
# google-cloud-storage>=2.0.0

# For analytics and data processing
# pandas>=2.0.0
# numpy>=1.24.0
pyarrow>=12.0.0

# ADBC (Arrow Database Connectivity) fast path for the database
# destination. Always installed in the Docker image; ``ADBC_FAST_PATH=1``
# at runtime is what gates use. pip consumers see ADBC as the optional
# ``adbc`` extra in pyproject.toml — the runtime image bundles it so a
# flag flip needs no rebuild.
adbc-driver-manager>=1.1.0
adbc-driver-postgresql>=1.1.0
# Snowflake: driver wheel only; URI builder registration tracked in #98.
adbc-driver-snowflake>=1.1.0

# For NoSQL databases
# motor>=3.0.0
# aioredis>=2.0.0

# For security features
# cryptography>=41.0.0
# passlib>=1.7.4

# For job scheduling
# apscheduler>=3.10.0