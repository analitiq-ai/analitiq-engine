# Base environment configuration for Docker deployment
# Copy this file to .env and configure your values
#
# Usage:
#   cd docker && PIPELINE_ID=mysql-to-postgresql docker compose run --rm source_engine

# === Environment Configuration ===
ENV=local

# === Pipeline Configuration ===
# PIPELINE_ID must match a pipeline_id in pipelines/manifest.json
PIPELINE_ID=

# === AWS Configuration (for dev/prod environments) ===
AWS_REGION=eu-central-1
# AWS_PROFILE=

# === S3 Buckets ===
ROW_COUNT_BUCKET=analitiq-client-pipeline-row-count

# === Logging ===
# LOG_LEVEL=INFO

# === Optional: Advanced Configuration ===
# DB_MIN_CONNECTIONS=2
# DB_MAX_CONNECTIONS=10